/// A config of default settings for the package
class DefaultSettings {
  /// A title for the report
  static const title = 'Dependencies Report';

  /// The directory to save the report
  static const outputDirectory = 'reports';

  /// The directory to save the report
  static const outputFilename = 'dependencies_report';

  /// Whether a pdf should be generated
  static const generatePdf = false;

  /// Whether a md should be generated
  static const generateMd = true;
}
